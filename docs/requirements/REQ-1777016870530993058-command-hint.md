# command hint

**ID**: REQ-1777016870530993058  
**Status**: CODE_REVIEW  
**Priority**: MEDIUM  
**Created**: 2026-04-24T07:47:50Z  

## Description

follow the '/start-work -> /work-on -> /code-review -> /worktree-merge' flow, at the end of the output of each command, recommend the user to run the next command in the flow

## Success Criteria

- [ ] Running `/start-work <REQ-ID>` includes a closing recommendation to run `/work-on <REQ-ID>` as the next step in the lifecycle.
- [ ] Running `/work-on <REQ-ID>` (when it advances to or targets review-ready output) includes a closing recommendation to run `/code-review <REQ-ID>`.
- [ ] Running `/code-review <REQ-ID>` includes a closing recommendation to run `/worktree-merge <REQ-ID>` after review actions are complete.
- [ ] Recommended next-command hints are omitted when the current command fails before reaching normal completion output.
- [ ] Command hints are concise, action-oriented, and consistently formatted across all four lifecycle commands.

## Technical Notes

- Update prompt files that drive each command flow to append a standardized "Next command" footer only on successful completion: `.github/prompts/start-work.prompt.md`, `.github/prompts/work-on.prompt.md`, `.github/prompts/code-review.prompt.md`, and `.github/prompts/worktree-merge.prompt.md`.
- Keep guidance aligned with lifecycle transitions already documented in orchestrator instructions (PROPOSED → IN_PROGRESS → CODE_REVIEW → MERGED) without changing transition rules.
- Reuse a shared phrasing pattern to avoid drift (e.g., "Next recommended command: ...") and reduce maintenance overhead.
- Validate that hints remain correct for edge cases such as `--no-auto` in `/work-on` and merge precondition failures in `/worktree-merge`.
- Risk: adding hints unconditionally may mislead users during error paths; implementation should gate hints on success outcomes.


## Development Plan

1. Update `.github/prompts/start-work.prompt.md` so successful `/start-work` output ends with a single next-step recommendation: `/work-on <REQ-ID>`.
2. Update `.github/prompts/work-on.prompt.md` so successful review-ready progression ends with `/code-review <REQ-ID>`, while preserving existing `--no-auto` behavior.
3. Update `.github/prompts/code-review.prompt.md` so successful completion ends with `/worktree-merge <REQ-ID>` and does not emit this hint on failure paths.
4. Update `.github/prompts/worktree-merge.prompt.md` to add a success-only follow-up recommendation that matches the lifecycle stage (e.g., deployment/status follow-up where applicable).
5. Validate behavior by running representative flows with `scripts/start-work.sh`, `scripts/worktree-merge.sh`, and `scripts/show-requirement.sh REQ-1777016870530993058`, then regenerate docs using `./scripts/regenerate-docs.sh` if manifests/spec status changed.

**Last updated**: 2026-04-24T07:49:42Z

## Dependencies

None

## Worktree

(Will be populated when work starts: feature/REQ-ID-slug)

---

* **Linked Worktree**: feature/REQ-1777016870530993058-command-hint
* **Branch**: feature/REQ-1777016870530993058-command-hint
* **Merged**: No
* **Deployed**: No
