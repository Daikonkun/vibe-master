# command hint

**ID**: REQ-1777016870530993058  
**Status**: MERGED  
**Priority**: MEDIUM  
**Created**: 2026-04-24T07:47:50Z  

## Description

follow the '/start-work -> /work-on -> /code-review -> /worktree-merge' flow, at the end of the output of each command, recommend the user to run the next command in the flow

## Success Criteria

- [x] Running `/start-work <REQ-ID>` includes a closing recommendation to run `/work-on <REQ-ID>` as the next step in the lifecycle.
- [x] Running `/work-on <REQ-ID>` (when it advances to or targets review-ready output) includes a closing recommendation to run `/code-review <REQ-ID>`.
- [x] Running `/code-review <REQ-ID>` includes a closing recommendation to run `/worktree-merge <REQ-ID>` after review actions are complete.
- [x] Recommended next-command hints are omitted when the current command fails before reaching normal completion output.
- [x] Command hints are concise, action-oriented, and consistently formatted across all four lifecycle commands.

## Technical Notes

- Update prompt files that drive each command flow to append a standardized "Next command" footer only on successful completion: `.github/prompts/start-work.prompt.md`, `.github/prompts/work-on.prompt.md`, `.github/prompts/code-review.prompt.md`, and `.github/prompts/worktree-merge.prompt.md`.
- Keep guidance aligned with lifecycle transitions already documented in orchestrator instructions (PROPOSED → IN_PROGRESS → CODE_REVIEW → MERGED) without changing transition rules.
- Reuse a shared phrasing pattern to avoid drift (e.g., "Next recommended command: ...") and reduce maintenance overhead.
- Validate that hints remain correct for edge cases such as `--no-auto` in `/work-on` and merge precondition failures in `/worktree-merge`.
- Risk: adding hints unconditionally may mislead users during error paths; implementation should gate hints on success outcomes.

## Development Plan

1. Add explicit success-path next-command guidance to `.github/prompts/start-work.prompt.md`.
2. Add explicit success-path next-command guidance to `.github/prompts/work-on.prompt.md` and keep trust/auto rules unchanged.
3. Add explicit success-path next-command guidance to `.github/prompts/code-review.prompt.md`.
4. Add explicit success-path next-command guidance to `.github/prompts/worktree-merge.prompt.md` with deployment-aware follow-up.
5. Standardize wording to `Next recommended command: ...` and verify hints are constrained to successful command outcomes.

## Dependencies

None

## Worktree

(Will be populated when work starts: feature/REQ-ID-slug)

---

* **Linked Worktree**: feature/REQ-1777016870530993058-command-hint
* **Branch**: feature/REQ-1777016870530993058-command-hint
* **Merged**: Yes
* **Deployed**: No
